/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package ai_interview.app;

import ai_interview.list.LinkedList;

import static ai_interview.utilities.StringUtils.join;
import static ai_interview.utilities.StringUtils.split;
import static ai_interview.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
